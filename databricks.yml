# This is a Databricks asset bundle definition for dq_tool_latest.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: dq_tool_latest

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-8829303634942508.8.azuredatabricks.net

  ## Optionally, there could be 'staging' or 'prod' targets here.
  #
  # prod:
  #   workspace:
  #     host: https://adb-8829303634942508.8.azuredatabricks.net
